	if (document.querySelector('.hiddenJson')) {
	//if ( document.getElementById('data-structure-markup-index-js-extra') ) {
		let data_structure_markup = `${bwetcDataStructure.markup}`;
		document.head.innerHTML += data_structure_markup;
	 }